<div id="nav-outer">
  <nav id="main-nav" class="outer">
    <!-- 只在移动端显示菜单按钮 -->
    <% if (theme.mobile_nav) { %>
      <a id="main-nav-toggle" class="nav-icon"></a>
      <% } %>

        <% for (var i in theme.menu){ %>
          <a class="main-nav-link" href="<%- url_for(theme.menu[i]) %>">
            <%= i %>
          </a>
          <% } %>

            <div class="main-nav-space-between"></div>

            <!-- Dark mode toggle -->
            <a class="dark-mode-toggle" href="javascript:void(0)" onclick="toggleDarkMode()" title="Toggle dark mode"></a>

            <!-- Language switcher -->
            <% if (page.layout==='post' || is_home()) { %>
              <a class="lang-switch" href="javascript:void(0)" onclick="switchLanguage()">
                EN
              </a>
              <% } %>

                <!-- RSS icon -->
                <% if (theme.rss){ %>
                  <a id="nav-rss-link" class="nav-icon" href="<%- url_for(theme.rss) %>"
                    title="<%= __('rss_feed') %>"></a>
                  <% } %>
  </nav>
</div>
<div id="header-title">
  <h1 id="logo-wrap">
    <a href="<%- url_for() %>" id="logo">
      <%= config.title.replace(/，/g, ', ' ).replace(/；/g, '; ' ).replace(/：/g, ': ' ).replace(/！/g, '! '
        ).replace(/‘/g, '` ' ).replace(/’/g, "' " ).replace(/“/g, ' “' ).replace(/”/g, "” " ).replace(/（/g, ' ('
        ).replace(/）/g, ') ' ).replace(/。/g, '. ' ).replace(/？/g, '? ' ) %>
    </a>
  </h1>
  <% if (theme.subtitle || config.subtitle){ %>
    <h2 id="subtitle-wrap">
      <a href="<%- url_for() %>" id="subtitle">
        <%= (theme.subtitle || config.subtitle).replace(/，/g, ', ' ).replace(/；/g, '; ' ).replace(/：/g, ': '
          ).replace(/！/g, '! ' ).replace(/‘/g, '` ' ).replace(/’/g, "' " ).replace(/“/g, ' “' ).replace(/”/g, "” "
          ).replace(/（/g, ' (' ).replace(/）/g, ') ' ).replace(/。/g, '. ' ).replace(/？/g, '? ' ) %>
      </a>
    </h2>
    <% } %>
</div>